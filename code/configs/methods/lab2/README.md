# Lab2 Configurations

These files are loaded by `scripts/run_lab2.sh`.

Lab2 corresponds to the random/shift dirty-data comparison in Figure `fig:random-errors`, Figure `fig:shift-errors`, and Table `tab:skin_covertype`.
